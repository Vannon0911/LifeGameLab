# progress

Original prompt: Implement the LifeGameLab V1 Freeze-Handover with fixed Main-Run rooms, preset-bound deterministic worldgen, contract-first new actions/fields, water/biome separation, labor isolation for legacy tools, and progression decoupled from HARVEST_CELL.

- 2026-03-14: Dokumentation auf LLM_ENTRY-basierte Struktur vollstaendig neu gesetzt.
- 2026-03-14: Historische Dokuinhalte konsolidiert und ersetzt.
- 2026-03-14: Naechster Schritt: groesste Datenquellen inventarisieren und Aufteilungs-/Verkleinerungsplan erstellen.
- 2026-03-14: UI (`ui.js`) entlang Verantwortlichkeiten weiter geschnitten (`ui.dom.js`, `ui.feedback.js`).
- 2026-03-14: `main.js` als reine Orchestrierung abgespeckt (`worldStateLog.js`, `reportUtils.js`, `bootStatus.js`).
- 2026-03-14: `step.js` weiter entmonolithisiert (`stepRuntime.js`) ohne Tick-Reihenfolgeaenderung.
- 2026-03-14: Tests in Klassen getrennt (`quick`, `truth`, `stress`) und Runner eingefuehrt.
- 2026-03-14: Laufzeitbudgets und Hotspot-Messung dokumentiert (`docs/TEST_BUDGETS.md`, `docs/PERF_HOTSPOTS.md`).
- 2026-03-14: Deterministisches Advisor-Read-Model als gemeinsame Source of Truth fuer `render_game_to_text`, HUD und Statuspanel eingefuehrt (`src/project/llm/advisorModel.js`, `src/project/llm/readModel.js`, `src/game/ui/ui.model.js`, `src/game/ui/ui.js`).
- 2026-03-14: Main-Run Defaults geschaerft: `SET_WIN_MODE` nach Tick 0 gesperrt, Placement Cost standardmaessig aktiv, Global Learning standardmaessig aus (`src/game/sim/reducer/controlActions.js`, `src/game/sim/reducer/index.js`, `src/project/contract/stateSchema.js`, `src/game/sim/reducer/techTreeOps.js`).
- 2026-03-14: Renderer-Overlays an `meta.activeOverlay` angebunden; `energy`, `toxin`, `nutrient`, `territory`, `conflict` liefern jetzt echte Diagnosebilder (`src/game/render/renderer.js`).
- 2026-03-14: Advisor-/Overlay-Vertraege und neue Main-Run Defaults mit fokussierten Tests abgesichert; `npm run test:quick` erfolgreich (`tests/test-advisor-model.mjs`, `tests/test-overlay-diagnostics.mjs`, aktualisierte Vertrags- und Interaktions-Tests).
- 2026-03-14: Playwright-Smoke gegen localhost aufgedeckt und behoben: Renderer brach im Feldsurface-Pfad mit `rv is not defined`; Fix in `src/game/render/renderer.js` validiert, UI rendert wieder, `advanceTime`/`render_game_to_text` laufen und der Energie-Overlay erzeugt im Pausenbild ein anderes Diagnosebild.
- 2026-03-14: Freeze-Rework gestartet: `worldPresetId`, `world.water`, `world.biomeId`, neue Main-Run-Actions und Progressionsfelder im Contract verankert.
- 2026-03-14: `worldgen.js` auf preset-gebundene deterministische Pipeline mit `river_delta`, `dry_basin`, `wet_meadow` umgestellt; `expandWorldPreserve()` kopiert jetzt `world.water` und `world.biomeId`.
- 2026-03-14: UI auf `lage/eingriffe/evolution/welt/labor` umgezogen; Main-Run nutzt `HARVEST_PULSE`, `PRUNE_CLUSTER`, `RECYCLE_PATCH`, `SEED_SPREAD`, Labor kapselt Render-/Overlay-/Brush-Diagnose.
- 2026-03-14: Freeze-Doku und fokussierte Tests hinzugefuegt (`docs/SESSION_HANDOFF.md`, `tests/test-freeze-contract.mjs`, `tests/test-freeze-progression.mjs`, `tests/test-world-presets-determinism.mjs`).
- 2026-03-14: Offene Punkte fuer Nacharbeit: Advisor-Texte und Rest-Legacy in `ui.js` weiter ausduennen, Playwright-Smoke fuer neue Panels wiederholen, groessere Truth-Suite nach Freeze-Refactor komplett durchlaufen lassen.
- 2026-03-14: Repo-eigener Playwright-Debug-Loop hinzugefuegt (`tools/playwright-debug-loop.mjs`, `docs/PLAYWRIGHT_DEBUG_LOOP.md`, `npm run debug:playwright`): setzt nach Start `ticks/s` hoch und loescht Browserdaten vor jedem Close.
- 2026-03-14: LLM_ENTRY-konformer Debug-Fix: `GEN_WORLD` setzt jetzt bootstrap-taugliche Sim-Metriken im operativen Reducerpfad; manueller Browser-Loop startet ohne falschen Kollaps-Nullzustand. Pflichtchecks gruen: drift-negative-order, determinism-long, test:quick, test:truth.
- 2026-03-14: Labor-/Benchmark-Loop nachgezogen: `ui.js` hoert jetzt live auf `benchmark:update`, `docs/PLAYWRIGHT_DEBUG_LOOP.md` auf manuellen Benchmark-Debug erweitert. Live verifiziert: Status `setup_main -> worker_init -> done`, Report sichtbar, JSON/CSV aktiv.
- 2026-03-14: Manueller 10x-UI-Debug-Loop gefahren. Reproduzierbarer Stale-Panel-Lag nach Welt-/Labor-Neustartpfaden behoben: Weltgroesse, Preset, Seed, Speed, Render- und Overlay-Wechsel triggern jetzt lokalen Panel-Repaint in `ui.js`; Vertragstest erweitert.
- 2026-03-14: LLM_ENTRY-konformer Debug-Fix: GEN_WORLD setzt jetzt bootstrap-taugliche Sim-Metriken im operativen Reducerpfad; manueller Browser-Loop startet ohne falschen Kollaps-Nullzustand. Pflichtchecks gruen: drift-negative-order, determinism-long, test:quick, test:truth.
- 2026-03-14: Kombinierter Debug-Run (Funktion -> UI -> UX -> Vollscan) abgeschlossen.
- 2026-03-14: Main-Run-Funktionsparameter getunt: `RECYCLE_PATCH` mit groesserem Wirkbereich und staerkerem Reclaim/DNA-Faktor; neuer harter Funktionsloop-Test (`tests/test-mainrun-function-loop.mjs`) hinzugefuegt und in `quick` aufgenommen.
- 2026-03-14: UI/UX-Sync nachgezogen: Panelwechsel persistiert `meta.ui.activeTab`/`meta.ui.panelOpen`; Main-Run-Text ohne Scanner/Overlay-Vokabular (`Naechste Labor-Diagnose`). Contract-Test erweitert.
- 2026-03-14: Phase A / Ticket A1 abgeschlossen: Contract-Surface fuer `GAME_MODE`, `RUN_PHASE`, `BRUSH_MODE.FOUNDER_PLACE`, `CONFIRM_FOUNDATION`, `meta.gameMode`, `sim.runPhase`, `sim.founderBudget`, `sim.founderPlaced`, `world.founderMask`, `world.visibility`, `world.explored` in IDs, State-Defaults, `simGate`, `metrics` und Contract-Tests verankert. Noch ohne Worldgen-, Runtime- oder UI-Umschaltung.
- 2026-03-14: Abbruch-Sicherung angelegt: `docs/PHASE_A_TODO.md` fuehrt die atomare Ticket-Reihenfolge A1-A12 mit Status, Scope-Gates und Nicht-Zielen; A1 ist als erledigt markiert.
- 2026-03-14: A1-Testhaertung nachgezogen: `test-dataflow-contract`, `test-ui-strategy-contract`, `test-string-contract`, `test-interactions` laufen gruen; `tests/test-interactions.mjs` auf robustes leeres Testtile statt fixer Koordinate umgestellt.
- 2026-03-14: Phase A / Ticket A2 abgeschlossen: `GEN_WORLD(payload?)` akzeptiert `{ gameMode }`, `generateWorld(..., { gameMode })` trennt Genesis-Start (ohne Player-/CPU-Cluster) von `LAB_AUTORUN`-Legacy-Seed; `world.founderMask`, `world.visibility`, `world.explored` werden beim Worldgen angelegt. Verifiziert mit Bootstrap-/Module-/Contract-/Gate-Tests.
- 2026-03-14: Phase A / Ticket A3 abgeschlossen: `applyWorldAi()` ist jetzt modusgebunden; `seedFoundersIfEmpty()` laeuft nur noch in `GAME_MODE.LAB_AUTORUN`, Standard-Genesis seeded nie Auto-Founder. Moduskontext wird von Reducer -> `simStep` -> `runWorldSystemsPhase` -> `applyWorldAi` durchgereicht; Tests erweitert und gruen.
- 2026-03-14: Phase A / Ticket A4 abgeschlossen: zentrale Laufregel `shouldAdvanceSimulation(state)` eingefuehrt und im Runtime-Loop genutzt; `TOGGLE_RUNNING(true)` in `GENESIS_SETUP`/`RESULT` als No-Op, `SIM_STEP` und `APPLY_BUFFERED_SIM_STEP` in `GENESIS_SETUP`/`RESULT` hart blockiert (auch mit `force:true`). Betroffene Quick-Tests explizit auf `GEN_WORLD({ gameMode: LAB_AUTORUN })` umgestellt; `npm run test:quick` wieder voll gruen.
- 2026-03-14: Phase A / Ticket A5 abgeschlossen: `WORLD_PRESETS` fuehren normierte `startWindows` (player/cpu), Diskretisierungsregel ist zentral ueber `getStartWindowRange()`/`isTileInStartWindow()` nutzbar und per neuem Vertragstest (`tests/test-world-start-windows.mjs`) fuer 16x16 und Preset-Konsistenz abgesichert.
- 2026-03-14: Phase A / Ticket A6 abgeschlossen: `PLACE_CELL` ist im Standardmodus waehrend `GENESIS_SETUP` jetzt Founder-Placement (`FOUNDER_PLACE`-Brush, nur `startWindows.player`, max `founderBudget`, founderMask/founderPlaced Tracking, Removal vor Confirm, keine DNA-Kosten). In `GAME_MODE.GENESIS` nach `RUN_ACTIVE` bleibt `PLACE_CELL` blockiert; Lab-Modus behaelt Legacy-Verhalten. Neues Testmodul `tests/test-founder-placement.mjs`, Quick-Suite weiterhin gruen.
- 2026-03-14: Phase A / Ticket A7 abgeschlossen: `CONFIRM_FOUNDATION` validiert jetzt exakt vier Founder (alive, player-owned, founderMask, im Startfenster, genau eine 8-Nachbarschaftskomponente) und setzt nur dann `runPhase=RUN_ACTIVE` plus `running=true`; andernfalls bleibt der Zustand unveraendert. Neuer Test `tests/test-confirm-foundation.mjs`, Quick-Suite gruen.
- 2026-03-14: Phase A / Ticket A8 abgeschlossen: In `GAME_MODE.GENESIS` + `RUN_PHASE.GENESIS_SETUP` sind Legacy/Main-Run-Altpfade jetzt hart gesperrt (`PLACE_SPLIT_CLUSTER`, `SET_ZONE`, `HARVEST_CELL`, `BUY_EVOLUTION`, `HARVEST_PULSE`, `PRUNE_CLUSTER`, `RECYCLE_PATCH`, `SEED_SPREAD` -> `[]`). Lab-Modus bleibt unveraendert. Neuer Gate-Test `tests/test-genesis-action-gates.mjs`, Quick-Suite gruen.
- 2026-03-14: Phase A / Ticket A9 abgeschlossen: Runtime-Caller getrennt. `recoverWorld(reason)`, `Benchmark.run()` und `DevBalance.onExtinction()` nutzen jetzt explizit `GEN_WORLD({ gameMode: LAB_AUTORUN })` plus `TOGGLE_RUNNING(true)`; Standard-Weltcaller in `ui.js` (Preset-/Groessenwechsel) autostarten nicht mehr und bleiben auf `GEN_WORLD()` im Genesis-Standardpfad. Quick-Suite gruen.
- 2026-03-14: Phase A / Ticket A10 abgeschlossen: UI-Mindestfluss fuer Genesis eingebaut: Founder-Brush-Handling inklusive Klick-Removal eigener Founder vor Confirm, Founder-Card mit Zaehler und `Gründung bestätigen`-Button, Genesis-Hinweise auf Play/Step sowie `getInfluencePhase(..., runPhase)` mit `Gruenden` in `GENESIS_SETUP`. Groessenauswahl enthaelt jetzt `16x16`, `speedForGrid(16)` ist auf 2 gesetzt. Dataflow-Dispatchquelle fuer `CONFIRM_FOUNDATION` nachgezogen, Quick-Suite gruen.
- 2026-03-14: Phase A / Ticket A11 abgeschlossen: `applyWinConditions()` setzt bei Sieg/Niederlage jetzt explizit `runPhase=RESULT`; `PLACE_CELL` ist in RESULT hart blockiert und `CONFIRM_FOUNDATION` bleibt dort No-Op. Neuer Test `tests/test-result-phase.mjs`, Quick-Suite gruen.
- 2026-03-14: Phase A / Ticket A12 abgeschlossen: Finale Test-/Smoke-/Doku-Haertung durchgezogen. Zusaetzliche Determinismus-/Loop-/Invarianten-/Security-Tests explizit auf `GEN_WORLD({ gameMode: LAB_AUTORUN })` umgestellt, Genesis-spezifische Tests separat gehalten. Abschlusslaeufe erfolgreich: `npm run test:quick`, `npm run test:truth`, `npm run test:stress`. Scope-Check bestanden (kein Phase-B-Code, kein `SET_GAME_MODE`, kein CPU-Spawn/Fog/Zone-Rework).
- 2026-03-14: Test-Runner entlastet: `npm test` laeuft bewusst nur noch `quick`; grosse Suiten (`truth`, `stress`) nur noch explizit via `npm run test:full` oder einzeln. LLM-Entry-Enforcement auf atomarer Ebene nachgezogen: `run-test-suite` fuehrt zusaetzlich vor jedem Testfile einen eigenen Preflight-`check` aus.
- 2026-03-14: Phase B / P0 Workflow-Entblockung abgeschlossen: `tools/llm-preflight.mjs` unterstuetzt jetzt `*`-Globmuster fuer Klassifizierungsregeln; `docs/llm/TASK_ENTRY_MATRIX.json` ordnet `src/game/contracts/*` dem Contract-Scope und `docs/PHASE_*_TODO.md` sowie Handoff-/Entry-Doku dem Versioning-Scope zu. Belegt mit `llm-preflight classify` fuer `src/game/contracts/ids.js`, `docs/PHASE_A_TODO.md`, virtuelle Phase-B/C-TODO-Pfade sowie `node tests/test-llm-contract.mjs`.
- 2026-03-14: Phase B / Ticket B1 abgeschlossen: `RUN_PHASE.GENESIS_ZONE` ist jetzt in `src/game/contracts/ids.js` contract-synchronisiert; `coreZoneMask`, `CONFIRM_CORE_ZONE` und die Unlock-/Bootstrap-Felder sind in Contract, Sim-Gates, Metrics und Bootstrap-State verankert. Fokussierte Nachweise gruen: `test-bootstrap-gen-world`, `test-freeze-contract`, `test-sim-gate`, `test-string-contract`.
- 2026-03-14: Phase B / Ticket B2 abgeschlossen: `CONFIRM_FOUNDATION` fuehrt jetzt in `GENESIS_ZONE` statt direkt in `RUN_ACTIVE`; `running` bleibt `false`, `coreZoneMask` bleibt leer, kein CPU-Spawn wird angestossen und Founder-Placement ist in `GENESIS_ZONE` gesperrt. Nachweise gruen: `test-confirm-foundation`, `test-founder-placement`, plus Contract-/String-Sync-Tests.
- 2026-03-14: Phase B / Ticket B3 abgeschlossen: `CONFIRM_CORE_ZONE` validiert die Founder-Komponente in `GENESIS_ZONE`, stempelt sie bitgenau nach `world.coreZoneMask`, setzt `unlockedZoneTier=1`, `nextZoneUnlockKind="DNA"`, `nextZoneUnlockCostEnergy` aus dem Preset und schaltet den Lauf explizit nach `RUN_ACTIVE` mit `running=true`. Nachweise gruen: `test-confirm-core-zone`, `test-confirm-foundation`, `test-sim-gate`.
- 2026-03-14: Phase B / Ticket B4 abgeschlossen: CPU-Bootstrap im Standardmodus ist jetzt ein einmaliger deterministischer Kernconfirm-Pfad. `seedDeterministicBootstrapCluster()` seeded erst nach erfolgreichem `CONFIRM_CORE_ZONE` in `startWindows.cpu`; `cpuBootstrapDone` blockiert Doppelseeds. Nachweise gruen: `test-confirm-core-zone`, `test-bootstrap-gen-world`, `run-test-suite quick`.
- 2026-03-14: Phase B / Ticket B5 abgeschlossen: `zoneUnlockProgress` tickt jetzt aus bestehender Energie (`playerEnergyStored / nextZoneUnlockCostEnergy`), `coreEnergyStableTicks` zaehlt nur bei positivem `playerEnergyNet` plus lebender Player-Zelle in `coreZoneMask`; keine neue Ressource eingefuehrt. Nachweise gruen: `test-confirm-core-zone`, `run-test-suite quick`.
- 2026-03-14: Phase B / Ticket B6 abgeschlossen: `GENESIS_ZONE` bleibt als harte Zwischenphase voll gesperrt (`PLACE_CELL`, Legacy-/Run-Actions, Step/Toggle), bis `CONFIRM_CORE_ZONE` explizit nach `RUN_ACTIVE` uebergibt. Gate-Test auf `GENESIS_SETUP` und `GENESIS_ZONE` erweitert; Quick-Suite gruen.
- 2026-03-14: Phase B / Ticket B7 abgeschlossen: UI kennt jetzt beide Genesis-Stufen explizit. `getInfluencePhase()` liefert `Gruenden` bzw. `Energiekern`; `ui.js` blockiert Play/Step in `GENESIS_ZONE` mit klaren Hinweisen, zeigt den zweiten Button `Energiekern bestaetigen` und blendet nach Kernconfirm das echte DNA-Unlock-Meter (`zoneUnlockProgress`, `coreEnergyStableTicks`) im Main-Run ein. `tests/test-genesis-ui-minimum.mjs` wurde erweitert und in `quick` aufgenommen.
- 2026-03-14: Nachlauf-Fix nach B7: `src/project/contract/dataflow.js` fuehrt `CONFIRM_CORE_ZONE` jetzt auch als UI-Dispatch-Quelle, damit Manifest-Dataflow und der reale UI-Button wieder deckungsgleich sind. Nachweise gruen: `test-dataflow-contract`, `run-test-suite quick`.
- 2026-03-14: Workflow-Fix vor B8: `src/app/main.js` war trotz Pflichtlektuere und Runtime-Orchestrierungsrolle nicht klassifizierbar. `docs/llm/TASK_ENTRY_MATRIX.json` ordnet `src/app/` jetzt dem UI-Task zu; `docs/llm/ui/UI_TASK_ENTRY.md` fordert `src/app/main.js` fuer Caller-/Boot-Tasks explizit mit. `tests/test-llm-contract.mjs` sichert die neue Klassifizierbarkeit ab.
- 2026-03-14: Phase B / Ticket B8 abgeschlossen: neue fokussierte Smokes decken jetzt sowohl den Standardpfad `Genesis -> Foundation -> Core -> Run` als auch die Runtime-Caller-Splittung in `src/app/main.js` ab. Hinzugefuegt: `tests/test-standard-genesis-flow.mjs` und `tests/test-main-runtime-callers.mjs`; beide in `quick` aufgenommen. Nachweise gruen: beide neuen Tests, `test-llm-contract`, `run-test-suite quick`.
- 2026-03-14: Phase B / Ticket B9 abgeschlossen: TODO-/Patchlog-Stand finalisiert, Scope erneut geprueft. Phase B endet ohne Pattern-/Fog-/Tech-Tree-/Zonen-Grossumbau und ohne CPU-Aenderung jenseits des einmaligen Kern-Bootstraps.
- 2026-03-14: P0-C Doku-Entblockung abgeschlossen: `docs/PHASE_C_TODO.md` als kanonische Phase-C-Arbeitsliste angelegt und in `docs/PHASE_A_TODO.md`, `docs/LLM_ENTRY.md` und `docs/PROJECT_STRUCTURE.md` verankert. Inhaltlich ist damit nur die Prozess-/Gate-Basis fuer den echten DNA-/Zone-2-Block gesetzt; es wurde bewusst noch kein Phase-C-Gameplay-Code implementiert.
- 2026-03-14: Phase C / Ticket C1 abgeschlossen: Contract-/State-Basis fuer DNA-Zone-Setup eingefuehrt. Neu im Contract: `RUN_PHASE.DNA_ZONE_SETUP`, `world.dnaZoneMask`, `sim.zone2Unlocked`, `sim.zone2PlacementBudget`, `sim.dnaZoneCommitted`, `sim.nextInfraUnlockCostDNA` sowie die no-op-Action-Surface `START_DNA_ZONE_SETUP`, `TOGGLE_DNA_ZONE_CELL`, `CONFIRM_DNA_ZONE`. `generateWorld()` und `expandWorldPreserve()` tragen die neue Maskenflaeche mit; `CONFIRM_CORE_ZONE` resetet die neuen Phase-C-Felder auf definierte Defaults. Nachweise gruen: `test-freeze-contract`, `test-sim-gate`, `test-confirm-core-zone`, `test-dataflow-contract`, `test-manifest-dataflow`, `test-string-contract`.
- 2026-03-14: Phase C / Tickets C2-C4 abgeschlossen: voller DNA-Pfad ist jetzt produktiv. `START_DNA_ZONE_SETUP` oeffnet `RUN_ACTIVE -> DNA_ZONE_SETUP` nur bei vollem DNA-Meter und presetgebundenem Budget; `TOGGLE_DNA_ZONE_CELL` erzwingt Ownership, Core-Exklusivitaet, 8er-Adjazenz und Removal vor Confirm; `CONFIRM_DNA_ZONE` validiert die 4er-Komponente, commitet `dnaZoneMask`, setzt `unlockedZoneTier=2`, schaltet auf `nextZoneUnlockKind="INFRA"` und kehrt explizit nach `RUN_ACTIVE` zurueck. Neue Nachweise gruen: `test-start-dna-zone-setup`, `test-toggle-dna-zone-cell`, `test-confirm-dna-zone`, `test-standard-dna-flow`.
- 2026-03-14: Phase C / Tickets C5-C8 abgeschlossen: bestaetigte DNA-Zone erzeugt jetzt deterministisch `playerDNA` aus lebenden Player-Kacheln in `dnaZoneMask`; `DNA_ZONE_SETUP` blockiert Legacy-/Run-Actions, Step und Toggle hart; UI zeigt DNA-Start-CTA, Setup-Zustand, Placement-Zaehler, Confirm-Button und Post-Commit-Hinweis auf Infrastruktur; Quick-Smokes decken den Standardpfad `Foundation -> Core -> DNA -> Run` mit ab. Nachweise gruen: `test-confirm-dna-zone`, `test-dna-zone-setup-gates`, `test-genesis-ui-minimum`, `test-ui-contract`, `test-standard-dna-flow`, `run-test-suite quick`.
- 2026-03-14: Phase C / Ticket C9 abgeschlossen: `docs/PHASE_C_TODO.md` ist vollstaendig auf erledigt gesetzt; Scope blieb eng (kein Infrastruktur-Bau, kein Pattern-/Fog-/Tech-Rework). Damit ist Phase C fachlich und prozessual abgeschlossen.
- 2026-03-14: Restphasen-Governance nachgezogen: `docs/PHASE_C_TODO.md` von veraltetem Prep-/Blocker-Kopf bereinigt; `docs/PHASE_D_TODO.md` als naechster produktiver Block bestaetigt; neue kanonische Folgephasen `docs/PHASE_E_TODO.md`, `docs/PHASE_F_TODO.md` und `docs/PHASE_G_TODO.md` angelegt und in `docs/LLM_ENTRY.md`, `docs/PROJECT_STRUCTURE.md` sowie `docs/SESSION_HANDOFF.md` fuer den Ticket-Loop verankert.
